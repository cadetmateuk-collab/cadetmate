import type { PhysicalProduct } from './types';

/**
 * Physical merchandise is not sold through the app yet.
 * Keep this list empty rather than inventing products or a checkout path.
 * When a catalogue exists, load it here and wire purchase to the real flow.
 */
export function getPhysicalProducts(): PhysicalProduct[] {
  return [];
}
