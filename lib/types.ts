// Shared flashcard types — re-exported from @cadet-mate/shared for web + mobile.
export type {
  CardType,
  Difficulty,
  PackStatus,
  FlashcardPack,
  Flashcard,
  CardProgress,
  PackStats,
  UserXP,
  StudyMode,
  SessionCardState,
  UserRole,
} from '@cadet-mate/shared';
